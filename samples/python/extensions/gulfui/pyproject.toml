[project]
name = "gulfui_ext"
version = "0.1.0"
description = "GulfUI Extension"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["a2a-sdk>=0.3.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
