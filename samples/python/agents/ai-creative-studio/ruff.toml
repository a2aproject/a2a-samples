line-length = 88
target-version = "py311"

[lint]
# Select core rules only - appropriate for a sample/demo project
select = ["E", "F", "I", "UP"]

# Ignore rules that are too strict for a sample project
ignore = [
    "E501",    # Line too long (handled by formatter)
]
